"""One-shot builder for the Russian-language PDF report.

Reads the executed English combined report, replaces each markdown cell's
source with a Russian translation, writes the result as
`censtrunc_report_ru.ipynb`, then converts to `censtrunc_report_ru.pdf`
via the standard webpdf route. Code cells, plot output and numeric tables
are inherited from the English report unchanged.

Usage
-----
    python examples/_build_report_ru.py            # build + convert to PDF
    python examples/_build_report_ru.py --no-pdf   # just write the .ipynb
"""

from __future__ import annotations

import argparse
import subprocess
import sys
from pathlib import Path

import nbformat as nbf

HERE = Path(__file__).resolve().parent
SOURCE = HERE / "censtrunc_report.ipynb"
TARGET_NB = HERE / "censtrunc_report_ru.ipynb"
TARGET_PDF = HERE / "censtrunc_report_ru.pdf"

# Russian translations for the markdown cells of the English report, indexed
# by their position among markdown cells in the executed combined report.
# Cell 0 is the print-CSS block and is left untouched.

RU_MARKDOWN: dict[int, str] = {}


def _set(idx: int, text: str) -> None:
    RU_MARKDOWN[idx] = text


# ----------------------------------------------------------------------
# Title, §1 introduction, §2 Python landscape
# ----------------------------------------------------------------------

_set(
    1,
    "# censtrunc\n"
    "## Цензурированная и усечённая нормальная регрессия с произвольными порогами\n"
    "\n"
    "**Автор:** Рамис Пайгин  \n"
    "**Пакет:** `censtrunc` v0.1.0  \n"
    "**Репозиторий:** [github.com/Ramis-Paygin/censored-truncated-regression]"
    "(https://github.com/Ramis-Paygin/censored-truncated-regression)  \n"
    "\n"
    "---\n"
    "\n"
    "### Аннотация\n"
    "\n"
    "`censtrunc` — Python-пакет для оценивания методом максимального"
    " правдоподобия двух семейств моделей с ограниченной зависимой переменной:"
    " (i) **цензурированной и усечённой нормальной регрессии** с произвольными"
    " левым и правым порогами, обобщающей классическую тобит-модель [1], и"
    " (ii) **модели Хекмана** [3], причём поддерживаются как двухшаговый метод" 
    " оценивания, так и совместное оценивание методом максимального"
    " правдоподобия. Пакет"
    " предоставляет программный интерфейс в стиле scikit-learn, итоговую"
    " таблицу результатов в стиле statsmodels, оценивание предельных"
    " эффектов через `get_margeff`, тест отношения правдоподобия с"
    " гипотезой в виде строки и поддержку формул в стиле patsy. В"
    " цензурированном случае используется глобально вогнутая параметризация"
    " Олсена [2].\n"
    "\n"
    "Отчёт построен по двум семействам моделей. Каждая часть содержит три"
    " раздела: **теория**, **примеры применения** библиотеки на"
    " сгенерированных и реальных данных, **эмпирическая проверка"
    " корректности**: аналитические предельные случаи и сравнение с"
    " независимыми реализациями на тех же данных. Полные библиографические"
    " ссылки приведены в конце отчёта.",
)

_set(
    2,
    "## 1. Введение\n"
    "\n"
    "Обычная линейная регрессия предполагает, что зависимая переменная"
    " наблюдается на всей вещественной оси. На практике она часто"
    " ограничена:\n"
    "\n"
    "- **Цензурирование**: значения вне интервала $(L, R)$ заменяются"
    " ближайшей границей. Типичные примеры: оценка удовлетворённости по"
    " шкале 0–10 или заработная плата, ограниченная установленным в"
    " организации максимальным уровнем оплаты труда.\n"
    "- **Усечение**: значения вне $(L, R)$ полностью отсутствуют в выборке."
    " Так устроены опросные данные, регистрирующие только респондентов с"
    " доходом выше определённого уровня, или административные данные о"
    " сделках выше порога обязательной отчётности.\n"
    "- **Селективные данные**: *зависимая* переменная наблюдается лишь"
    " тогда, когда срабатывает отдельное *уравнение отбора*; значения"
    " регрессоров доступны для всех наблюдений — и тех, где зависимая"
    " переменная наблюдается, и тех, где нет (это отличает селективные"
    " данные от усечённой выборки, в которой пропущенные наблюдения"
    " полностью исключаются). Классический пример — зарплата: она"
    " фиксируется только у занятых, тогда как социально-демографические"
    " характеристики (образование, опыт, возраст, число детей в семье)"
    " известны и для незанятых.\n"
    "\n"
    "Метод наименьших квадратов даёт смещённые оценки во всех трёх случаях."
    " Классические решения: **тобит-модель** [1] для одностороннего"
    " цензурирования в нуле, **усечённая нормальная регрессия** для"
    " одностороннего усечения и **модель Хекмана** [3] для третьего"
    " случая.\n"
    "\n"
    "`censtrunc` объединяет все три модели в одной Python-библиотеке. Все"
    " они используют согласованный программный интерфейс для оценивания,"
    " построения прогнозов и интерпретации результатов (`predict`,"
    " `get_margeff`, `lr_test`, `from_formula`). Разделы 3 и 4 разбирают"
    " эти три модели по двум классам: цензурированную и усечённую"
    " регрессию — в части А, модель Хекмана — в части Б.",
)

_set(
    3,
    "## 2. Что уже реализовано в Python\n"
    "\n"
    "В R соответствующие модели реализованы давно и удобно: пакет"
    " `survival::survreg` (на нём построен `AER::tobit`) покрывает"
    " цензурированный случай, `truncreg::truncreg` — усечённый, а"
    " `sampleSelection::selection` [7] — обе версии оценки модели"
    " Хекмана. В Python соответствующие реализации разрознены и"
    " покрывают эти модели лишь частично.\n"
    "\n"
    "| Пакет | Цензурирование / тобит | Усечение | Модель Хекмана | Замечания |\n"
    "|---|---|---|---|---|\n"
    "| **statsmodels** | — | — | — | Моделей с ограниченной зависимой переменной нет. |\n"
    "| **scikit-learn** | — | — | — | Моделей с ограниченной зависимой переменной нет. |\n"
    "| **linearmodels** | — | — | — | Моделей с ограниченной зависимой переменной нет. |\n"
    "| **lifelines** | цензурирование слева в модели Вейбулла и логнормальной модели ускоренного времени отказа (AFT — accelerated failure time) | — | — | Пакет для моделирования времени до некоторого события (например, отказа оборудования). Цензурирование там — частный случай, не подходит для произвольных порогов $L, R$. |\n"
    "| **scikit-survival** | цензурирование справа, модели ускоренного времени отказа | — | — | Тот же класс задач, что у lifelines: моделирование времени до события. Произвольные пороги $L, R$ не задаются. |\n"
    "| **py4etrics** | `Tobit` с произвольными $L, R$ | `Truncreg` с произвольными $L, R$ | двухшаговый `Heckit` (значение `method='mle'` по умолчанию игнорируется) | Учебный пакет; предельных эффектов, теста отношения правдоподобия и формул нет. |\n"
    "| **stnwanekezie/TobitRegression** | один класс `Tobit` поверх `statsmodels.OLS` | — | — | Скрипт около 425 строк; есть только таблица оценок коэффициентов, ни прогнозов, ни предельных эффектов. |\n"
    "| **PyMC / bambi** | байесовский вариант через `Censored` | байесовский вариант через `Truncated` | можно собрать вручную | Байесовские пакеты: модели задаются вручную и оцениваются совсем другим способом — не максимизацией правдоподобия, а случайным сэмплированием апостериорного распределения. |\n"
    "| **marginaleffects** | — | — | — | Инструмент для расчёта предельных эффектов и сравнения уже оценённых моделей; сам моделей не оценивает. |\n"
    "\n"
    "### Что нового добавляет `censtrunc`\n"
    "\n"
    "- **Двустороннее цензурирование и усечение** с произвольными границами $L$ и"
    " $R$ во всех трёх оценщиках (односторонний случай получается как"
    " частный).\n"
    "- **Совместная оценка модели Хекмана методом максимального"
    " правдоподобия** (как у `sampleSelection::selection`), а не только"
    " двухшаговый алгоритм; для двухшагового реализован парный бутстрап"
    " стандартных ошибок.\n"
    "- **Предельные эффекты** через единый `get_margeff` в стиле"
    " statsmodels для всех трёх моделей. Для цензурированных моделей"
    " дополнительно доступны три предельных эффекта вероятностей"
    " попадания в левую, внутреннюю и правую зоны — $P(Y=L)$,"
    " $P(L<Y<R)$, $P(Y=R)$; для модели Хекмана — четыре эффекта"
    " (`latent`, `conditional`, `unconditional`, `prob-selected`).\n"
    "- **Тест отношения правдоподобия** (*likelihood ratio test, LR-тест*) в двух вариантах: между парой"
    " оценённых моделей либо `model.lr_test(hypotheses)` со строкой"
    " гипотез (`'(x1 = 0), (x2 = x3)'`, `'2*x1 + x4 = 1'`).\n"
    "- **Формулы в стиле patsy** поддерживаются во всех трёх случаях.\n"
    "- Единый `predict()` возвращает любое подмножество из шести величин"
    " через однобуквенную строку `kind`: для цензурированной модели это"
    " `'hctlmr'`, для модели Хекмана — `'snpohu'`.\n"
    "- **Сравнение с независимыми реализациями** в наборе тестов"
    " (`py4etrics`, `sampleSelection` из R) — см. §3.3 и §4.3.",
)


# ----------------------------------------------------------------------
# Part A: Censored/Truncated  (theory, applications, validation)
# ----------------------------------------------------------------------


_set(
    4,
    "---\n"
    "\n"
    "# Часть А. Цензурированная и усечённая нормальная регрессия",
)


_set(
    5,
    "## 3.1 Теория\n"
    "\n"
    "### Латентная регрессия\n"
    "\n"
    "Обе модели используют одну и ту же спецификацию латентной"
    " линейной регрессии с нормально распределёнными ошибками:\n"
    "\n"
    "$$Y^* = X'\\beta + e, \\qquad e \\mid X \\sim \\mathcal{N}(0, \\sigma^2).$$\n"
    "\n"
    "Различие между ними заключается в правиле, связывающем латентную"
    " переменную $Y^*$ с наблюдаемыми данными.\n"
    "\n"
    "### Цензурированная модель\n"
    "\n"
    "Значения за пределами интервала $(L, R)$ заменяются ближайшей"
    " границей:\n"
    "\n"
    "$$Y = \\min\\bigl(R,\\; \\max(L,\\; Y^*)\\bigr).$$\n"
    "\n"
    "Обозначим\n"
    "\n"
    "$$\\alpha_L = \\frac{L - X'\\beta}{\\sigma}, \\qquad"
    " \\alpha_R = \\frac{R - X'\\beta}{\\sigma}.$$\n"
    "\n"
    "Тогда логарифм правдоподобия объединяет вероятностную"
    " массу в точках цензурирования с плотностью нормального"
    " распределения внутри интервала:\n"
    "\n"
    "$$\\ell(\\beta, \\sigma) = \\sum_{Y_i = L} \\log\\Phi(\\alpha_{L,i})\n"
    "+ \\sum_{Y_i = R} \\log\\bigl[1 - \\Phi(\\alpha_{R,i})\\bigr]\n"
    "+ \\sum_{L < Y_i < R} \\Bigl[\\log\\phi(\\alpha_i) - \\log\\sigma\\Bigr].$$\n"
    "\n"
    "Здесь $\\phi$ и $\\Phi$ — соответственно функция плотности и функция"
    " распределения стандартного нормального распределения.\n"
    "\n"
    "Классическая тобит-модель является частным случаем при"
    " $L = 0$, $R = +\\infty$.\n"
    "\n"
    "### Параметризация Олсена\n"
    "\n"
    "Оптимизация ведётся в координатах $\\gamma = \\beta/\\sigma$,"
    " $\\nu = 1/\\sigma$. В работе [2] доказано, что в этих координатах"
    " логарифм правдоподобия **глобально вогнут**, поэтому любой"
    " градиентный метод сходится к единственному максимуму. После"
    " сходимости выполняется обратное преобразование к $(\\beta, \\sigma)$,"
    " а стандартные ошибки оценок коэффициентов рассчитываются по"
    " наблюдаемой информационной матрице.\n"
    "\n"
    "### Усечённая модель\n"
    "\n"
    "В усечённой выборке наблюдений вне интервала $(L, R)$ нет вовсе."
    " Плотность $Y$ при условии $L < Y^* < R$ — это плотность нормального"
    " распределения, делённая на вероятность попадания внутрь интервала:\n"
    "\n"
    "$$f(y_i \\mid L < Y_i^* < R) =\n"
    "\\frac{\\sigma^{-1}\\,\\phi\\!\\bigl((y_i - X_i'\\beta)/\\sigma\\bigr)}\n"
    "     {\\Phi(\\alpha_{R,i}) - \\Phi(\\alpha_{L,i})}.$$\n"
    "\n"
    "### Условные средние и предельные эффекты\n"
    "\n"
    "Вычисляются три условных средних (вывод см. в [10, гл. 27]):\n"
    "\n"
    "**Латентное среднее** — математическое ожидание ненаблюдаемой"
    " переменной $Y^*$ при фиксированном $X$:\n"
    "\n"
    "$$\\mathbb{E}[Y^* \\mid X] = X'\\beta.$$\n"
    "\n"
    "**Цензурированное среднее** — математическое ожидание наблюдаемой"
    " $Y$ с учётом точечных масс на порогах:\n"
    "\n"
    "$$\\mathbb{E}[Y \\mid X] = L\\,\\Phi(\\alpha_L)\n"
    "+ R\\,[1 - \\Phi(\\alpha_R)]\n"
    "+ X'\\beta\\,[\\Phi(\\alpha_R) - \\Phi(\\alpha_L)]\n"
    "+ \\sigma\\,[\\phi(\\alpha_L) - \\phi(\\alpha_R)].$$\n"
    "\n"
    "**Усечённое среднее** — математическое ожидание $Y$ при условии,"
    " что наблюдение попало внутрь интервала:\n"
    "\n"
    "$$\\mathbb{E}[Y \\mid X,\\, L < Y < R] = X'\\beta"
    " + \\sigma\\,\\frac{\\phi(\\alpha_L) - \\phi(\\alpha_R)}\n"
    "                  {\\Phi(\\alpha_R) - \\Phi(\\alpha_L)}.$$\n"
    "\n"
    "Введём обозначение: $X_i = (X_{i1}, \\dots, X_{ip})'$ — вектор"
    " значений $p$ регрессоров для наблюдения $i$ (без свободного члена),"
    " $X_{ij}$ — значение $j$-го регрессора для наблюдения $i$."
    " Соответствующие коэффициенты — $\\beta_1, \\dots, \\beta_p$;"
    " свободный член обозначается $\\beta_0$, так что всего модель"
    " содержит $p+1$ коэффициент."
    " Предельные эффекты доступны в двух формах:\n"
    "\n"
    "- **AME** (*Average Marginal Effect*) — производная условного"
    " среднего по $j$-му регрессору, вычисленная в каждом наблюдении и"
    " усреднённая по выборке:\n"
    "\n"
    "$$\\mathrm{AME}_j = \\frac{1}{n}\\sum_{i=1}^n"
    " \\frac{\\partial\\,\\mathbb{E}[Y \\mid X = X_i]}{\\partial X_{ij}};$$\n"
    "\n"
    "- **MEM** (*Marginal Effect at the Mean*) — та же производная,"
    " но вычисленная один раз в точке средних значений регрессоров"
    " $\\bar X = \\tfrac{1}{n}\\sum_{i=1}^{n} X_i$:\n"
    "\n"
    "$$\\mathrm{MEM}_j ="
    " \\frac{\\partial\\,\\mathbb{E}[Y \\mid X = \\bar X]}{\\partial \\bar X_j}.$$\n"
    "\n"
    "Дифференцируя три условных средних, выписанных выше, по $j$-му"
    " регрессору, получаем явные формулы для частных производных"
    " (составных частей AME и MEM):\n"
    "\n"
    "$\\dfrac{\\partial\\,\\mathbb{E}[Y^* \\mid X]}{\\partial X_j} = \\beta_j,$\n"
    "\n"
    "$\\dfrac{\\partial\\,\\mathbb{E}[Y \\mid X]}{\\partial X_j}"
    " = \\beta_j\\,[\\Phi(\\alpha_R) - \\Phi(\\alpha_L)],$\n"
    "\n"
    "$\\dfrac{\\partial\\,\\mathbb{E}[Y \\mid X,\\, L < Y < R]}{\\partial X_j}"
    " = \\beta_j\\,\\bigl(1 - \\delta_{LR}(X)\\bigr),$\n"
    "\n"
    "где\n"
    "\n"
    "$$\\delta_{LR}(X) = \\frac{\\alpha_R\\phi(\\alpha_R) -"
    " \\alpha_L\\phi(\\alpha_L)}{\\Phi(\\alpha_R) - \\Phi(\\alpha_L)}"
    " + \\left\\{\\frac{\\phi(\\alpha_L) -"
    " \\phi(\\alpha_R)}{\\Phi(\\alpha_R) - \\Phi(\\alpha_L)}\\right\\}^2$$\n"
    "\n"
    "— поправка на усечение. В частности, цензурированный предельный"
    " эффект — это латентный коэффициент $\\beta_j$, умноженный на"
    " вероятность того, что наблюдение окажется внутри интервала"
    " $(L, R)$.\n"
    "\n"
    "Стандартные ошибки оценок $\\widehat{\\mathrm{AME}}_j$ и"
    " $\\widehat{\\mathrm{MEM}}_j$ вычисляются дельта-методом по"
    " ковариационной матрице оценок параметров $(\\hat\\beta, \\hat\\sigma)$:"
    " предельный эффект — нелинейная функция оценок, и его асимптотическая"
    " дисперсия получается стандартной линеаризацией этой функции вокруг"
    " истинных значений.\n"
    "\n"
    "### Статистический вывод\n"
    "\n"
    "Для каждой оценённой модели рассчитываются два критерия качества:\n"
    "\n"
    "- **псевдо-$R^2$ Мак-Фаддена** — аналог коэффициента детерминации"
    " для моделей, оцениваемых методом максимального правдоподобия."
    " Он показывает, насколько полная модель лучше объясняет данные по"
    " сравнению с нулевой (только с константой). Значения, близкие к"
    " единице, свидетельствуют о хорошем качестве модели. Формула:\n"
    "\n"
    "    $$R^2_{\\text{McFadden}} = 1 -"
    " \\frac{\\ell_{\\text{full}}}{\\ell_{\\text{null}}},$$\n"
    "\n"
    "    где $\\ell_{\\text{full}}$ — логарифм правдоподобия оцениваемой"
    " модели (со всеми регрессорами), а $\\ell_{\\text{null}}$ — логарифм"
    " правдоподобия модели, содержащей только константу.\n"
    "- **LR-тест против нулевой модели** (только с константой) —"
    " проверяет совместную значимость всех регрессоров, то есть нулевую гипотезу"
    " $H_0: \\beta_1 = \\dots = \\beta_p = 0$ (свободный член $\\beta_0$"
    " не накладывается). Это аналог $F$-теста на"
    " общую значимость регрессии в МНК.\n"
    "\n"
    "Для проверки более общих гипотез о коэффициентах (одновременное"
    " равенство нулю или линейные ограничения) применяется метод"
    " `lr_test`: он сравнивает две вложенные модели и возвращает"
    " статистику"
    " $LR = 2(\\ell_{\\text{full}} - \\ell_{\\text{restricted}})"
    " \\xrightarrow{d} \\chi^2_q$ (асимптотически, при справедливости"
    " ограничений), где $q$ — число накладываемых ограничений.",
)


_set(
    6,
    "## 3.2 Применение\n"
    "\n"
    "Четыре подробно разобранных примера демонстрируют возможности"
    " библиотеки на постепенно усложняющихся данных:\n"
    "\n"
    "- 3.2.1 двустороннее цензурирование на сгенерированных данных,\n"
    "- 3.2.2 классическая тобит-модель с цензурированием слева на данных Fair [5] —"
    " опросе 1969 года о супружеских изменах среди 6366 респондентов,\n"
    "- 3.2.3 усечённая регрессия на сгенерированных данных,\n"
    "- 3.2.4 сравнение цензурирования и усечения на одних и тех же данных.",
)


_set(7, "---\n\n### 3.2.1 Двусторонняя цензурированная регрессия")

_set(
    8,
    "## Процесс генерации данных\n"
    "\n"
    "$$Y_i^* = 1.0 + 0.5\\,X_{1i} - 0.3\\,X_{2i} + 0.2\\,X_{3i} + e_i,\\quad"
    " e_i \\sim \\mathcal N(0, 1).$$\n"
    "\n"
    "Наблюдаемая величина цензурирована: значения ниже $L=0$ и выше"
    " $R=2.5$ заменяются ближайшей границей. Это типичная картина"
    " двустороннего цензурирования, например, балл удовлетворённости по"
    " шкале 0–2.5.",
)

_set(
    9,
    "## Распределения латентной и наблюдаемой переменных\n"
    "\n"
    "Скопления на порогах $L$ и $R$ в наблюдаемом распределении —"
    " характерный признак двустороннего цензурирования.",
)

_set(
    10,
    "## Оценивание цензурированной регрессии\n"
    "\n"
    "По умолчанию используется параметризация Олсена: в ней логарифм"
    " правдоподобия глобально вогнут [2], что гарантирует сходимость"
    " любого градиентного оптимизатора.",
)

_set(
    11,
    "## Сравнение с методом наименьших квадратов\n"
    "\n"
    "На цензурированных данных метод наименьших квадратов смещён [4]:"
    " оценки коэффициентов смещаются к нулю примерно"
    " пропорционально вероятности цензурирования. Оценка тобит-модели"
    " методом максимального правдоподобия в нашей реализации состоятельна.",
)

_set(
    12,
    "## Прогнозы: шесть величин одной командой\n"
    "\n"
    "`model.predict(X)` возвращает DataFrame со всеми шестью величинами, среди которых"
    " три условных средних и три вероятности: $P(Y=L)$,"
    " $P(L<Y<R)$ и $P(Y=R)$. У"
    " каждой колонки — однобуквенная мнемоника:\n"
    "\n"
    "- `h` — *hidden*, латентное среднее $\\mathbb E[Y^* \\mid X]$\n"
    "- `c` — *censored*, цензурированное среднее $\\mathbb E[Y \\mid X]$\n"
    "- `t` — *truncated*, усечённое среднее $\\mathbb E[Y \\mid X, L<Y<R]$\n"
    "- `l` — вероятность того, что $Y$ примет значение $L$:"
    " $P(Y=L \\mid X) = \\Phi(\\alpha_L)$\n"
    "- `m` — вероятность того, что $Y$ попадёт внутрь интервала"
    " $(L, R)$: $P(L<Y<R \\mid X) = \\Phi(\\alpha_R) - \\Phi(\\alpha_L)$\n"
    "- `r` — вероятность того, что $Y$ примет значение $R$:"
    " $P(Y=R \\mid X) = 1 - \\Phi(\\alpha_R)$",
)

_set(
    13,
    "Подмножество колонок задаётся теми же буквами. Например, только три"
    " вероятности (по построению их сумма равна 1) либо только латентное"
    " среднее как одномерный массив:",
)

_set(
    14,
    "Длинные имена: `kind='latent'`, `'censored'`, `'truncated'` — также"
    " допустимы и для обратной совместимости возвращают одномерный массив.",
)

_set(
    15,
    "## Предельные эффекты через `get_margeff`\n"
    "\n"
    "Интерфейс предельных эффектов повторяет `get_margeff` из statsmodels:"
    " метод с тремя параметрами: `at`, `method`, `kind`."
    " Итоговая таблица оформлена в привычном для statsmodels виде.\n"
    "\n"
    "- **`at`** — точка, в которой берётся производная:\n"
    "    - `'overall'` — *AME* (Average Marginal Effect): предельный эффект"
    " вычисляется по каждому наблюдению и усредняется;\n"
    "    - `'mean'` — *MEM* (Marginal Effect at the Mean): предельный эффект"
    " вычисляется один раз в точке средних значений регрессоров;\n"
    "    - `'median'` — аналог *MEM* в точке медианных значений регрессоров.\n"
    "- **`method`** — вид эффекта:\n"
    "    - `'dydx'` — обычная производная (по умолчанию);\n"
    "    - `'eyex'` — эластичность"
    " $\\tfrac{\\partial Y}{\\partial X}\\cdot\\tfrac{X}{Y}$: на сколько"
    " процентов изменится $Y$ при изменении регрессора на один процент;\n"
    "    - `'dyex'`, `'eydx'` — две полуэластичности, в которых процентное"
    " изменение применяется только к одной из переменных.\n"
    "- **`kind`** — условное среднее, для которого считается эффект:\n"
    "    - `'latent'` — латентное $\\mathbb{E}[Y^*\\mid X]$;\n"
    "    - `'censored'` — цензурированное $\\mathbb{E}[Y\\mid X]$ (по умолчанию);\n"
    "    - `'truncated'` — усечённое $\\mathbb{E}[Y\\mid X,\\, L<Y<R]$.\n"
    "\n"
    "Для дискретных регрессоров $0/1$ доступен `dummy=True`: вместо"
    " производной возвращается разность значений $Y$ при $X=1$ и при $X=0$"
    " — содержательный «предельный эффект» в бинарном случае.",
)

_set(
    16,
    "Ниже сопоставляются четыре варианта предельного эффекта: **латентный**,"
    " два **цензурированных** (AME и MEM) и **усечённый** (AME)."
    " У латентного эффекта производная $\\partial\\,\\mathbb{E}[Y^* \\mid"
    " X]/\\partial X_j = \\beta_j$ не зависит от $X$, поэтому AME и MEM для"
    " него совпадают и оба равны самой оценке коэффициента"
    " $\\hat\\beta_j$ (отсюда и подпись `(=beta)` у соответствующего"
    " столбца). Латентный эффект играет роль удобного ориентира —"
    " сравнение показывает, насколько сильно цензурирование «сжимает»"
    " предельный эффект относительно латентного, а усечение — ещё"
    " сильнее.",
)

_set(
    17,
    "Тот же предельный эффект, но в виде эластичности (`method='eyex'`):",
)

_set(
    18,
    "### Предельные эффекты вероятностей попадания в левую, внутреннюю и правую зоны\n"
    "\n"
    "Помимо предельных эффектов условных средних, можно вычислить, как"
    " регрессор влияет на вероятности зон. Они задаются параметром `kind`:\n"
    "\n"
    "- `'prob-left'` — эффект на $P(Y=L) = \\Phi(\\alpha_L)$"
    " (попадание в левый порог);\n"
    "- `'prob-interior'` — эффект на"
    " $P(L<Y<R) = \\Phi(\\alpha_R) - \\Phi(\\alpha_L)$"
    " (попадание внутрь интервала);\n"
    "- `'prob-right'` — эффект на $P(Y=R) = 1 - \\Phi(\\alpha_R)$"
    " (попадание в правый порог).\n"
    "\n"
    "Поскольку три вероятности в сумме дают единицу, их предельные"
    " эффекты по построению складываются в ноль.",
)

_set(
    19,
    "Положительный коэффициент перемещает наблюдения от левого порога к"
    " правому: эффект на $P(Y=L)$ отрицателен, на $P(Y=R)$ положителен."
    " Столбец `sum (=0)` подтверждает, что ограничение «сумма равна нулю»"
    " выполняется численно.",
)

_set(
    20,
    "## Тест отношения правдоподобия\n"
    "\n"
    "Проверим, нужен ли регрессор `x3`: оценим модель с"
    " ограничением $\\beta_{x_3}=0$ и сравним значения логарифмов"
    " правдоподобия полной и ограниченной моделей.",
)

_set(
    21,
    "Малое p-value отвергает ограничение, то есть переменную `x3` нельзя"
    " считать избыточной. В рассматриваемом процессе генерации данных"
    " истинный коэффициент при `x3` равен 0.2, а выборка велика, поэтому"
    " тест корректно отвергает гипотезу.",
)

_set(
    22,
    "### LR-тест с гипотезой в виде строки\n"
    "\n"
    "Явное оценивание ограниченной модели удобно лишь в простых"
    " случаях, например, «исключить один регрессор». Для совместных или"
    " составных линейных ограничений `model.lr_test(hypotheses)`"
    " копирует интерфейс `f_test` из statsmodels, а именно принимает линейное"
    " ограничение в виде строки: одиночное, совместное или с арифметическими"
    " выражениями от имён параметров.",
)

_set(
    23,
    "При каждом вызове задача оптимизации решается заново с линейным"
    " ограничением $R\\hat\\theta = r$ (через `scipy.optimize` и"
    " `LinearConstraint`), а в качестве результата возвращается"
    " асимптотическая LR-статистика"
    " $2(\\ell_{\\text{full}} - \\ell_{\\text{rest.}}) \\xrightarrow{d}"
    " \\chi^2_q$.",
)


_set(
    24,
    "---\n\n### 3.2.2 Классическая тобит-модель на данных Fair о супружеских изменах",
)

_set(
    25,
    "## Спецификация\n"
    "\n"
    "Датасет Fair [5] содержит данные опроса 1969 года: 6366 респондентов"
    " указали число случаев супружеской измены за прошлый год вместе с"
    " набором демографических и семейных характеристик. Зависимая"
    " переменная цензурирована слева в нуле (около 68% респондентов"
    " сообщили о нулевом числе случаев). Следуя оригинальной работе,"
    " построим регрессию числа случаев измены от стандартного набора"
    " демографических и семейных признаков.",
)

_set(
    26,
    "## Оценивание\n"
    "\n"
    "Левый порог `CensoredRegression` равен нулю, правый порог не"
    " задаётся.",
)

_set(
    27,
    "## Предельные эффекты\n"
    "\n"
    "Поскольку зависимая переменная цензурирована слева на нуле,"
    " цензурированные предельные эффекты по модулю меньше латентных."
    " Латентные (нецензурированные) коэффициенты относятся к латентной"
    " величине $Y^*$ — ненаблюдаемой количественной мере склонности к"
    " измене; цензурированные предельные эффекты относятся к ожидаемому"
    " числу измен — наблюдаемой величине.",
)

_set(
    28,
    "## Сопоставление с МНК\n"
    "\n"
    "На цензурированной выборке метод наименьших квадратов занижает"
    " оценки коэффициентов [4]; оценка тобит-модели методом максимального"
    " правдоподобия это смещение устраняет.",
)


_set(29, "---\n\n### 3.2.3 Усечённая регрессия")

_set(
    30,
    "## Генерация усечённой выборки\n"
    "\n"
    "Сгенерируем большую генеральную совокупность и оставим только"
    " наблюдения с $0 < Y^* < 2.5$. В рассматриваемом процессе генерации"
    " данных двустороннее усечение «переживают» около 60 % исходных"
    " наблюдений.",
)

_set(31, "## Оценивание усечённой регрессии")

_set(
    32,
    "## Сопоставление с МНК\n"
    "\n"
    "Метод наименьших квадратов на усечённой выборке трактует её как"
    " обычную выборку из неограниченной генеральной совокупности — в"
    " итоге оценки коэффициентов смещены и, как правило, сжаты к нулю.",
)

_set(
    33,
    "## Прогнозы\n"
    "\n"
    "Для усечённой модели содержательный смысл имеют только две"
    " величины. Поскольку точечных масс на порогах нет, вероятности"
    " $P(Y=L)$, $P(L<Y<R)$, $P(Y=R)$ не определяются:\n"
    "\n"
    "- `h` (`latent`): $X'\\beta$ — среднее в генеральной совокупности;\n"
    "- `t` (`truncated`):"
    " $\\mathbb E[Y \\mid X, L<Y<R] = X'\\beta"
    " + \\sigma\\,\\dfrac{\\phi(\\alpha_L) - \\phi(\\alpha_R)}"
    "{\\Phi(\\alpha_R) - \\Phi(\\alpha_L)}$"
    " — среднее по наблюдаемым данным.",
)


_set(34, "---\n\n### 3.2.4 Усечение и цензура на одних и тех же данных")

_set(
    35,
    "Чтобы наглядно сопоставить два режима наблюдения: усечение и"
    " цензуру — генерируется единая латентная переменная $Y^* = X + e$."
    " **Усечённая выборка:** наблюдения с $y \\notin (L, R)$ полностью"
    " исключены. **Цензурированная выборка:** те же наблюдения заменены"
    " ближайшей границей. Линейная зависимость $Y^* = X$ в обоих случаях"
    " одна и та же. Простой вид $Y^* = X$ выбран для наглядности"
    " графика; результаты переносятся на любую линейную спецификацию."
    " Сравнение на двух соседних подграфиках позволяет проиллюстрировать"
    " различия в формах смещения, которые в численных таблицах"
    " незаметны.",
)

_set(
    36,
    "Построим доверительную полосу прогнозов с учётом асимптотической"
    " неопределённости оценок параметров. Возьмём 200 векторов параметров"
    " из их асимптотического нормального распределения, для каждого"
    " вычислим кривую условного среднего"
    " и нарисуем эти 200 кривых полупрозрачными. Скопления на"
    " порогах цензурирования выделены красным на правом графике.",
)

_set(
    37,
    "**На что обратить внимание.**\n"
    "\n"
    "- Чёрная линия — истинная зависимость $Y^* = X$.\n"
    "- Синяя полоса — оценённый методом максимального правдоподобия"
    " прогноз условного среднего с учётом асимптотической"
    " неопределённости оценок параметров. На **левом** подграфике"
    " оценённое условное среднее отклоняется внутрь интервала $(L, R)$, поскольку"
    " усечённое среднее $\\mathbb E[Y \\mid X, L<Y<R]$ смещается к центру"
    " интервала; на **правом** подграфике оценка становится более пологой у порогов,"
    " так как цензурированное среднее $\\mathbb E[Y\\mid X]$ совмещает"
    " латентную линейную часть с точечными массами.\n"
    "- Красные точки на правом подграфике — цензурированные наблюдения,"
    " попавшие на $Y=L$ и $Y=R$. В усечённой выборке аналогичных"
    " наблюдений нет, поскольку при усечении они полностью исключены.\n"
    "- Оценённые кривые близки к истинной во внутренней области, где"
    " данных больше всего; у обеих падает точность вблизи и за"
    " порогами, но ни одна не испытывает столь сильного смещения оценок к"
    " нулю, как метод наименьших квадратов.",
)


_set(
    38,
    "---\n\n## 3.3 Эмпирическая проверка корректности\n"
    "\n"
    "Три независимые проверки оценки максимального правдоподобия в"
    " цензурированной модели:\n"
    "\n"
    "1. **Предел без цензуры** — если пороги отодвинуть за пределы"
    " диапазона данных, ни одно наблюдение не попадает под цензуру, и"
    " оценка должна совпасть с оценкой методом наименьших квадратов.\n"
    "2. **Предел пробит-модели** — при $L \\approx R$ цензурированный логарифм"
    " правдоподобия сводится к логарифму правдоподобия пробит-модели для индикатора"
    " $\\mathbf 1\\{Y^* > c\\}$, причём оценки коэффициентов связаны"
    " соотношением"
    " $\\hat\\beta_{\\text{probit}} = \\hat\\beta_{\\text{tobit}}/\\hat\\sigma_{\\text{tobit}}$"
    " [10, §27.4].\n"
    "3. **Сравнение с независимыми реализациями в R** — пакет"
    " `survival::survreg` (на нём построен `AER::tobit`) и"
    " `truncreg::truncreg` решают ту же задачу максимизации"
    " правдоподобия. Две корректные реализации должны прийти в одну и"
    " ту же точку с точностью оптимизатора.",
)

_set(
    39,
    "## Без цензуры $\\Rightarrow$ МНК\n"
    "\n"
    "Возьмём `left=-1e6`, `right=1e6` — ни одно наблюдение не попадёт"
    " под цензуру. Цензурированный логарифм правдоподобия в этом пределе"
    " сводится к гауссовому, а его максимум совпадает с оценкой по"
    " методу наименьших квадратов.",
)

_set(
    40,
    "Значения в колонках совпадают до нескольких знаков. Дополнительно"
    " измерим максимальное расхождение и проверим, совпадают ли"
    " оценки параметра масштаба и логарифма правдоподобия."
    " Используется оценка масштаба методом максимального"
    " правдоподобия $\\hat\\sigma = \\sqrt{\\mathrm{SSR}/n}$,"
    " где ${\\mathrm{SSR}}$ — сумма квадратов остатков (sum of squared residuals).",
)

_set(
    41,
    "Все расхождения — в пределах погрешности численного оптимизатора."
    " В отсутствие цензуры оценщик сводится к методу наименьших"
    " квадратов, как и должно быть; следовательно, логарифм"
    " правдоподобия и его оптимизация реализованы корректно.",
)

_set(
    42,
    "## С цензурой: совпадение с R и расхождение с МНК\n"
    "\n"
    "В этом разделе используем явно цензурированные данные. Для каждого"
    " коэффициента сравниваются четыре числа:\n"
    "\n"
    "- **true** — истинное значение параметра;\n"
    "- **censtrunc** — оценка методом максимального правдоподобия из"
    " нашего пакета;\n"
    "- **R survreg** — гауссова реализация в `survreg` (на ней построен"
    " `AER::tobit`), посчитанная полностью независимым кодом;\n"
    "- **OLS** — оценка методом наименьших квадратов, смещённая при"
    " цензуре.\n"
    "\n"
    "Две корректные реализации оценки максимального правдоподобия должны"
    " прийти к *одному и тому же* единственному максимуму, так что"
    " оценки из `censtrunc` и из R должны совпадать с точностью"
    " оптимизатора, а оценка наименьших квадратов — отличаться от обеих"
    " и быть смещённой.",
)

_set(
    43,
    "Значения в столбцах `censtrunc` и `R` совпадают примерно до шести"
    " знаков, и при этом $\\max|\\text{censtrunc} - R|$ — малое ненулевое"
    " число порядка $10^{-8}$: два независимых оптимизатора сошлись к"
    " одному и тому же максимуму, оставаясь не побитово идентичными. Обе"
    " оценки приближаются к истинным значениям коэффициентов, тогда как"
    " оценка наименьших квадратов заметно смещена к нулю — это и есть"
    " известное сжатие оценок при цензуре, описанное в [4].",
)

_set(
    44,
    "## Предел пробит-модели: при узких порогах тобит-модель сводится к пробит-модели\n"
    "\n"
    "Возьмём цензурированную регрессию с $L = R = c$. Когда наблюдения"
    " внутри интервала $(L, R)$ отсутствуют, логарифм правдоподобия"
    " приводится к виду\n"
    "\n"
    "$$\\ell(\\beta, \\sigma) = \\sum_{i=1}^{n} \\mathbf 1\\{y_i = c\\}\\,"
    "\\log\\Phi\\!\\left(\\tfrac{c - X_i'\\beta}{\\sigma}\\right) +"
    "\\mathbf 1\\{y_i > c\\}\\,\\log\\!\\left[1 - \\Phi\\!\\left(\\tfrac{c - X_i'\\beta}{\\sigma}\\right)\\right],$$\n"
    "\n"
    "а это в точности логарифм правдоподобия пробит-модели для индикатора"
    " $D_i = \\mathbf 1\\{Y^*_i > c\\}$. При этом параметры пробит-модели"
    " совпадают с приведёнными к единичной дисперсии параметрами тобит-модели:"
    " $\\beta_{\\text{probit}} = \\beta_{\\text{tobit}}/\\sigma_{\\text{tobit}}$"
    " [10, §27.4].\n"
    "\n"
    "Положить $L = R$ буквально нельзя — при отсутствии внутреннего"
    " интервала"
    " параметр $\\sigma$ перестаёт быть отдельно идентифицируемым."
    " Однако достаточно узкого интервала вокруг $c$, а именно"
    " $[c - \\varepsilon, c + \\varepsilon]$, чтобы немногочисленные"
    " внутренние наблюдения восстановили идентификацию $\\sigma$, а"
    " остальные данные вели себя как бинарный исход. При"
    " $\\varepsilon\\to 0$ должно выполняться"
    " $\\hat\\beta_{\\text{tobit}}/\\hat\\sigma_{\\text{tobit}}\\to"
    " \\hat\\beta_{\\text{probit}}$.",
)

_set(
    45,
    "По мере сужения интервала $[c-\\varepsilon, c+\\varepsilon]$"
    " отношение оценок тобит-модели к оценке масштаба приближается к"
    " пробит-оценке. Оценка свободного члена смещается сильнее, чем"
    " оценки коэффициентов при регрессорах — это эффект, обусловленный"
    " конечной шириной интервала, величина которого имеет порядок"
    " $\\varepsilon/\\sigma$. Уже при $\\varepsilon = 0.05$ оценки"
    " коэффициентов согласуются с пробит-оценками с точностью до"
    " нескольких процентов. Тем самым проверяется и часть формулы"
    " правдоподобия, отвечающая за граничные члены, помимо"
    " проверки «без цензуры», сделанной выше.",
)


_set(
    46,
    "---\n"
    "\n"
    "# Часть Б. Модель Хекмана",
)


_set(
    47,
    "## 4.1 Теория\n"
    "\n"
    "### Задача с отбором\n"
    "\n"
    "Пусть зависимая переменная $Y$ наблюдается только тогда, когда"
    " индивид *попадает* в выборку по ненаблюдаемому правилу."
    " Классический пример — уравнение зарплаты. В качестве зависимой"
    " переменной берётся не сама зарплата $W$, а её логарифм"
    " $Y = \\log W$: это стандартный приём, предложенный Минсером [11]."
    " Логарифм зарплаты наблюдается лишь у занятых. Общая модель имеет"
    " вид (в уравнении зарплаты $Y^*$ — латентный $\\log W$,"
    " наблюдаемый только у занятых)\n"
    "\n"
    "$$\n"
    "Y^* = X'\\beta + e, \\qquad S^* = Z'\\gamma + u, \\qquad"
    " S = \\mathbf 1\\{S^* > 0\\},\n"
    "$$\n"
    "$$\n"
    "Y = \\begin{cases} Y^* & \\text{если } S = 1, \\\\"
    " \\text{не наблюдается} & \\text{если } S = 0, \\end{cases}"
    " \\qquad\n"
    "(e, u) \\sim \\mathcal N\\!\\left(0,\n"
    " \\begin{pmatrix}\\sigma^2 & \\rho\\sigma \\\\ \\rho\\sigma & 1\\end{pmatrix}\\right).\n"
    "$$\n"
    "\n"
    "Дисперсия $u$ нормирована на единицу для идентифицируемости"
    " (стандартное пробит-соглашение). При $\\rho \\ne 0$ оценка методом"
    " наименьших квадратов на отобранной подвыборке оказывается"
    " смещённой, поскольку\n"
    "\n"
    "$$\\mathbb E[Y \\mid X, Z, S=1] = X'\\beta + \\rho\\sigma\\,\\lambda(Z'\\gamma),$$\n"
    "\n"
    "где $\\lambda(t) = \\phi(t)/\\Phi(t)$ — обратный коэффициент Миллса."
    " Если опустить $\\lambda(Z'\\gamma)$ в регрессии (как делает обычный"
    " метод наименьших квадратов), получается классическое смещение"
    " из-за пропущенной переменной.\n"
    "\n"
    "### Двухшаговый алгоритм Хекмана\n"
    "\n"
    "1. **Пробит-регрессия** $S$ по $Z$ даёт оценку $\\hat\\gamma$.\n"
    "2. **Обратный коэффициент Миллса** $\\hat\\lambda_i ="
    " \\phi(Z_i'\\hat\\gamma)/\\Phi(Z_i'\\hat\\gamma)$ вычисляется для"
    " отобранных наблюдений.\n"
    "3. **Регрессия методом наименьших квадратов** $y_i$ по"
    " $(X_i, \\hat\\lambda_i)$ на отобранной подвыборке даёт оценки"
    " $\\hat\\beta$ и $\\hat\\rho\\hat\\sigma$.\n"
    "\n"
    "Стандартные ошибки второго шага без коррекции занижены: они"
    " игнорируют дисперсию $\\hat\\gamma$, привносимую пробит-шагом."
    " Парный бутстрап (`model.bootstrap`) восполняет этот пробел.\n"
    "\n"
    "### Совместная оценка методом максимального правдоподобия\n"
    "\n"
    "Полный логарифм правдоподобия [10, §27.10]:\n"
    "\n"
    "$$\\begin{aligned}\n"
    "\\ell &= \\sum_{S_i=0} \\log\\bigl[1 - \\Phi(Z_i'\\gamma)\\bigr] \\\\\n"
    "&\\quad + \\sum_{S_i=1}\\!\\bigg\\{\\log\\Phi\\!\\left("
    "\\frac{Z_i'\\gamma + (\\rho/\\sigma)(Y_i - X_i'\\beta)}"
    "{\\sqrt{1 - \\rho^2}}\\right) \\\\\n"
    "&\\hspace{6.5em} - \\tfrac12\\log(2\\pi\\sigma^2)"
    " - \\frac{(Y_i - X_i'\\beta)^2}{2\\sigma^2}\\bigg\\}.\n"
    "\\end{aligned}$$\n"
    "\n"
    "Оценка асимптотически эффективна, но несколько медленнее"
    " двухшагового алгоритма. Внутренняя оптимизация использует $\\rho ="
    " \\tanh(\\eta)$, чтобы условие $|\\rho| < 1$ не требовало"
    " явного ограничения.\n"
    "\n"
    "### Шесть прогнозных величин\n"
    "\n"
    "`HeckitRegression.predict()` возвращает шесть величин через"
    " однобуквенный код `kind`; по умолчанию `'snpohu'` возвращает все"
    " шесть в виде DataFrame.\n"
    "\n"
    "**Уравнение отбора** (использует $Z$):\n"
    "\n"
    "- **`s`** — вероятность отбора в выборку $\\Phi(Z'\\hat\\gamma)$\n"
    "- **`n`** — вероятность не попасть в выборку $1 - \\Phi(Z'\\hat\\gamma)$\n"
    "- **`p`** — латентная переменная $Z'\\hat\\gamma$\n"
    "\n"
    "**Уравнение исхода** (использует $X$, иногда $Z$):\n"
    "\n"
    "- **`o`** — условное среднее у отобранных наблюдений"
    " $\\mathbb E[Y \\mid X, Z, S=1] = X'\\hat\\beta + \\hat\\rho\\hat\\sigma\\,\\lambda(Z'\\hat\\gamma)$\n"
    "- **`h`** — латентное безусловное среднее"
    " $\\mathbb E[Y^* \\mid X] = X'\\hat\\beta$\n"
    "- **`u`** — условное среднее у неотобранных наблюдений"
    " $\\mathbb E[Y^* \\mid X, Z, S=0] = X'\\hat\\beta - \\hat\\rho\\hat\\sigma\\,\\phi(Z'\\hat\\gamma)/[1-\\Phi(Z'\\hat\\gamma)]$\n"
    "\n"
    "### Предельные эффекты\n"
    "\n"
    "Четыре стандартных вида предельных эффектов [9, §19.5] доступны через"
    " `model.get_margeff(kind=...)`:\n"
    "\n"
    "| `kind` | Differentiates |\n"
    "|---|---|\n"
    "| `'latent'` | $E[Y^*\\mid X] = X'\\beta$ |\n"
    "| `'conditional'` | $E[Y\\mid X, Z, S=1] = X'\\beta + \\rho\\sigma\\,\\lambda(Z'\\gamma)$ |\n"
    "| `'unconditional'` | $E[Y\\cdot S\\mid X, Z] = \\Phi(Z'\\gamma)X'\\beta + \\rho\\sigma\\,\\phi(Z'\\gamma)$ |\n"
    "| `'prob-selected'` | $P(S=1\\mid Z) = \\Phi(Z'\\gamma)$ |\n"
    "\n"
    "При $\\delta(t) = \\lambda(t)(t + \\lambda(t))$ (так что"
    " $\\mathrm d\\lambda/\\mathrm dt = -\\delta$) частная производная"
    " условного среднего по переменной $v$ имеет вид\n"
    "\n"
    "$$\\frac{\\partial E[Y\\mid S=1]}{\\partial v} = \\beta_v\\,\\mathbf 1\\{v\\in X\\}"
    " \\;-\\; \\gamma_v\\,\\rho\\sigma\\,\\delta(Z'\\gamma)\\,\\mathbf 1\\{v\\in Z\\}.$$\n"
    "\n"
    "Переменные, входящие в оба уравнения (идентифицируются по"
    " совпадению имён), дают вклад в предельный эффект и со стороны X, и"
    " со стороны Z; обе составляющие складываются. Стандартные ошибки"
    " получаются дельта-методом по оцененной совместной ковариационной"
    " матрице.",
)


_set(
    48,
    "## 4.2 Применение\n"
    "\n"
    "Демонстрация на сгенерированных данных, а затем — на классическом"
    " наборе Mroz [6]. Это данные опроса 1976 года о 753 замужних женщинах;"
    " 428 из них экономически активны, у остальных 325 зарплата отсутствует."
    " Ниже показано:"
    " как получать оценки двухшаговым методом и совместным методом"
    " максимального правдоподобия, как запрашивать шесть прогнозных"
    " величин, как пользоваться формулами в стиле patsy, как получать"
    " корректные стандартные ошибки двухшаговой оценки парным"
    " бутстрапом и как читать таблицу предельных эффектов.",
)


_set(
    49,
    "## Почему OLS на отобранной подвыборке смещён\n"
    "\n"
    "При $\\rho > 0$ ошибки $u$ и $e$ коррелированы, поэтому наблюдения"
    " с высоким $e$ в среднем имеют высокий $u$ и потому чаще попадают"
    " в выборку. Условное среднее на отобранных наблюдениях принимает"
    " вид\n"
    "\n"
    "$$\\mathbb E[Y \\mid X, S = 1] = X'\\beta + \\rho\\sigma\\,\\lambda(Z'\\gamma),$$\n"
    "\n"
    "где $\\lambda(\\cdot)$ — обратный коэффициент Миллса. Если опустить"
    " $\\lambda(Z'\\gamma)$ в регрессии (как делает наивный OLS), получается"
    " классическое смещение из-за пропущенной переменной.",
)

_set(
    50,
    "## Двухшаговый алгоритм Хекмана\n"
    "\n"
    "Алгоритм состоит из трёх шагов:\n"
    "\n"
    "1. Пробит-регрессия $S$ по $Z$ даёт оценку $\\hat\\gamma$.\n"
    "2. Для отобранных наблюдений вычисляется обратный коэффициент"
    " Миллса $\\hat\\lambda_i = \\phi(Z_i'\\hat\\gamma)/\\Phi(Z_i'\\hat\\gamma)$.\n"
    "3. Регрессия методом наименьших квадратов $y_i$ по"
    " $(X_i, \\hat\\lambda_i)$ на отобранной подвыборке даёт оценки"
    " $\\hat\\beta$ и $\\hat\\rho\\hat\\sigma$.",
)

_set(
    51,
    "## Совместный метод максимального правдоподобия\n"
    "\n"
    "Максимизируется совместный логарифм правдоподобия [10, §27.10]:\n"
    "\n"
    "$$\\begin{aligned}\n"
    "\\ell &= \\sum_{S_i=0} \\log\\bigl[1 - \\Phi(Z_i'\\gamma)\\bigr] \\\\\n"
    "&\\quad + \\sum_{S_i=1}\\!\\bigg\\{\\log\\Phi\\!\\left("
    "\\frac{Z_i'\\gamma + (\\rho/\\sigma)(Y_i - X_i'\\beta)}"
    "{\\sqrt{1 - \\rho^2}}\\right) \\\\\n"
    "&\\hspace{6.5em} - \\tfrac12\\log(2\\pi\\sigma^2)"
    " - \\frac{(Y_i - X_i'\\beta)^2}{2\\sigma^2}\\bigg\\}.\n"
    "\\end{aligned}$$\n"
    "\n"
    "Метод асимптотически эффективен, но несколько медленнее"
    " двухшагового.",
)

_set(
    52,
    "## Параллельное сравнение оценок\n"
    "\n"
    "Сопоставим три оценки на одних и тех же данных. Двухшаговый алгоритм"
    " Хекмана и совместная оценка методом максимального правдоподобия"
    " приближаются к истинному значению $\\beta_{\\text{shared}}$"
    " заметно ближе, чем метод наименьших квадратов, и одновременно дают"
    " оценки $\\rho$ и $\\sigma$.",
)

_set(
    53,
    "## Шесть видов прогноза\n"
    "\n"
    "Модель Хекмана допускает шесть содержательно различных прогнозов,"
    " доступных через один `predict()`. Каждой величине соответствует"
    " однобуквенный код; их можно комбинировать в произвольном порядке."
    " Здесь $\\lambda(t) = \\phi(t)/\\Phi(t)$ — обратный коэффициент"
    " Миллса.\n"
    "\n"
    "**Уравнение отбора** (используется $Z$):\n"
    "\n"
    "- **`s`** — вероятность отбора в выборку: $P(S=1 \\mid Z) = \\Phi(Z'\\hat\\gamma)$\n"
    "- **`n`** — вероятность не попасть в выборку: $P(S=0 \\mid Z) = 1 - \\Phi(Z'\\hat\\gamma)$\n"
    "- **`p`** — латентная переменная: $Z'\\hat\\gamma$\n"
    "\n"
    "**Уравнение исхода** (используется $X$, иногда оба):\n"
    "\n"
    "- **`o`** — $\\mathbb E[Y \\mid X, Z, S=1] = X'\\hat\\beta + \\hat\\rho\\hat\\sigma\\,\\lambda(Z'\\hat\\gamma)$ — условное среднее для отобранных наблюдений\n"
    "- **`h`** — $\\mathbb E[Y^* \\mid X] = X'\\hat\\beta$ — безусловное латентное среднее\n"
    "- **`u`** — $\\mathbb E[Y^* \\mid X, Z, S=0] = X'\\hat\\beta - \\hat\\rho\\hat\\sigma\\,\\phi(Z'\\hat\\gamma)/[1-\\Phi(Z'\\hat\\gamma)]$ — условное среднее для неотобранных наблюдений\n"
    "\n"
    "Вызов `predict(X=..., Z=...)` без `kind` возвращает все шесть"
    " колонок DataFrame в порядке `s, n, p, o, h, u`. Подмножество"
    " задаётся буквенной строкой: `kind='sn'`, `kind='ohu'` и т. д. Один"
    " символ возвращает одномерный массив. Длинные имена `'outcome'`,"
    " `'selection_prob'`, `'conditional'` сохранены для обратной"
    " совместимости и эквивалентны `'h'`, `'s'` и `'o'`.",
)

_set(
    54,
    "Три колонки уравнения исхода связаны законом полного математического"
    " ожидания:\n"
    "\n"
    "$$\\begin{aligned}\n"
    "\\underbrace{\\mathbb{E}[Y^* \\mid X]}_{\\texttt{h}}"
    " &= \\underbrace{\\Phi(Z'\\hat\\gamma)}_{\\texttt{s}}"
    " \\cdot \\underbrace{\\mathbb{E}[Y \\mid X, Z, S=1]}_{\\texttt{o}} \\\\\n"
    "&\\quad + \\underbrace{[1-\\Phi(Z'\\hat\\gamma)]}_{\\texttt{n}}"
    " \\cdot \\underbrace{\\mathbb{E}[Y^* \\mid X, Z, S=0]}_{\\texttt{u}},\n"
    "\\end{aligned}$$\n"
    "\n"
    "то есть безусловное латентное среднее (`h`) — это взвешенная сумма"
    " матожиданий для занятых (`o`) и незанятых (`u`) с весами `s` и `n`.",
)

_set(
    55,
    "## Та же оценка через формулу в стиле patsy\n"
    "\n"
    "Вместо того чтобы собирать $Y$, $X$, $Z$ вручную, можно"
    " воспользоваться конструктором `from_formula(...)` в стиле"
    " statsmodels. Для модели Хекмана нужны две формулы — отдельно для"
    " уравнения исхода и для уравнения отбора, с общим датафреймом:",
)

_set(
    56,
    "## Бутстрап стандартных ошибок\n"
    "\n"
    "Стандартные ошибки второго шага двухшагового алгоритма без коррекции"
    " занижены: они игнорируют дисперсию $\\hat\\gamma$ с пробит-шага."
    " Парный бутстрап даёт корректные стандартные ошибки. Стандартные"
    " ошибки совместной оценки методом максимального правдоподобия,"
    " полученные из наблюдаемой информационной матрицы, уже корректны.",
)

_set(
    57,
    "## Предельные эффекты\n"
    "\n"
    "У модели Хекмана четыре стандартных вида предельных эффектов"
    " [9, §19.5]:\n"
    "\n"
    "| `kind` | Differentiates |\n"
    "|---|---|\n"
    "| `'latent'` | $E[Y^*\\mid X] = X'\\beta$ |\n"
    "| `'conditional'` | $E[Y\\mid X, Z, S=1] = X'\\beta + \\rho\\sigma\\,\\lambda(Z'\\gamma)$ |\n"
    "| `'unconditional'` | $E[Y\\cdot S\\mid X, Z] = \\Phi(Z'\\gamma)X'\\beta + \\rho\\sigma\\,\\phi(Z'\\gamma)$ |\n"
    "| `'prob-selected'` | $P(S=1\\mid Z) = \\Phi(Z'\\gamma)$ |\n"
    "\n"
    "Если переменная входит в *оба* уравнения (в обсуждаемом ниже"
    " примере такая переменная названа `shared` — общий регрессор),"
    " итоговый предельный эффект складывается из двух составляющих:"
    " вклада со стороны уравнения исхода и вклада со стороны уравнения"
    " отбора. Стандартные ошибки вычисляются дельта-методом по"
    " оцененной совместной ковариационной матрице; для двухшаговой"
    " оценки они недоступны (`NaN`), так как совместная ковариационная"
    " матрица в явной аналитической форме недоступна — нужен бутстрап.\n"
    "\n"
    "Аналитические выражения для производных по конкретному наблюдению,"
    " где $\\lambda = \\phi/\\Phi$ и $\\delta(t) = \\lambda(t)(t + \\lambda(t))$,"
    " так что $\\mathrm d\\lambda/\\mathrm dt = -\\delta$:\n"
    "\n"
    "$$\\frac{\\partial E[Y\\mid S=1]}{\\partial v} = \\beta_v\\,\\mathbf 1\\{v\\in X\\}"
    " \\;-\\; \\gamma_v\\,\\rho\\sigma\\,\\delta(Z'\\gamma)\\,\\mathbf 1\\{v\\in Z\\}.$$\n"
    "\n"
    "Передавая `feature_names_outcome` и `feature_names_selection`, мы"
    " указываем библиотеке, что переменные с одинаковыми именами в $X$"
    " и в $Z$ — это одна и та же переменная.",
)

_set(
    58,
    "Все четыре вида предельных эффектов сведены в одну таблицу. Каждая"
    " строка усреднена по выборке (*AME*); пропуск в"
    " ячейке означает, что переменная не входит в это уравнение.",
)

_set(
    59,
    "Анализ столбцов показывает:\n"
    "\n"
    "- колонка `latent` воспроизводит оценки коэффициентов $\\beta$"
    " (проверка корректности);\n"
    "- `conditional` отличается от `latent` на общей переменной `shared`"
    " (добавляется поправка из уравнения отбора $Z$) и совпадает с"
    " ним на переменной `x_only`, которая входит только в уравнение"
    " исхода (поправки нет);\n"
    "- `unconditional` равномерно занижен: в $E[Y\\cdot S]$ слагаемое"
    " $X'\\beta$ умножается на вероятность отбора $\\Phi(Z'\\gamma)$,"
    " а не на единицу;\n"
    "- `prob-selected` содержит только переменные уравнения отбора,"
    " умноженные на $\\phi(Z'\\gamma)$.",
)

_set(
    60,
    "---\n## Реальные данные: Mroz (1976)\n"
    "\n"
    "Данные [6] — опрос 1976 года, 753 замужних женщины."
    " 428 из них (`inlf = 1`) работали;"
    " переменная `lwage` содержит логарифм их зарплаты. Остальные 325"
    " не работали, и зарплата для них не наблюдается."
    " МНК по 428 работающим отвечает на условный вопрос"
    " — *чем определяется зарплата среди занятых?*, тогда как на более"
    " широкий вопрос — *какую зарплату получали бы женщины, если бы"
    " работали все?* — отвечает модель Хекмана. Пример воспроизводит"
    " спецификацию из учебника Wooldridge [8, пример 17.5]:\n"
    "\n"
    "- **Уравнение исхода:** `lwage ~ educ + exper + expersq`\n"
    "- **Уравнение отбора:** `inlf ~ educ + exper + expersq + nwifeinc + age + kidslt6 + kidsge6`\n"
    "\n"
    "Переменные `nwifeinc`, `age`, `kidslt6`, `kidsge6` — исключаемые"
    " переменные (exclusion restrictions): они влияют на *решение работать*,"
    " но не должны напрямую влиять на зарплату.",
)

_set(
    61,
    "**Шаг 1 — оценка методом наименьших квадратов на работающих.**"
    " Такой результат получается, если игнорировать проблему отбора."
    " Ниже мы сравним его коэффициент при `educ` с оценкой,"
    " скорректированной по Хекману.",
)

_set(
    62,
    "**Шаг 2 — двухшаговый алгоритм Хекмана.** В уравнение отбора"
    " входят исключаемые переменные; на втором шаге к уравнению зарплаты"
    " добавляется обратный коэффициент Миллса.",
)

_set(63, "**Шаг 3 — совместный метод максимального правдоподобия** (асимптотически эффективен).")

_set(
    64,
    "### Сравнение коэффициентов\n"
    "\n"
    "Сопоставим три оценки коэффициентов уравнения зарплаты. По данным"
    " учебника Wooldridge [8], оценка отдачи от образования по МНК"
    " составляет около 0,108; оценка методом Хекмана близка к ней."
    " Оценённая корреляция $\\hat\\rho$ между ошибкой уравнения зарплаты"
    " и ошибкой уравнения отбора показывает, насколько силён механизм"
    " отбора.",
)

_set(
    65,
    "Интерпретация $\\hat\\rho$: положительное (отрицательное) значение"
    " означает, что ненаблюдаемые факторы, повышающие зарплату,"
    " одновременно повышают (понижают) вероятность занятости. Если"
    " $\\hat\\rho$ мал или статистически незначим (это покажет тест"
    " Вальда либо LR-тест на $\\rho = 0$), поправка на отбор"
    " эмпирически не нужна — достаточно обычного МНК.",
)

_set(
    66,
    "### Предельные эффекты на данных Mroz\n"
    "\n"
    "Для содержательной интерпретации обычно нужны предельные эффекты"
    " на условную зарплату (`cond E[lwage]`) и на вероятность занятости"
    " (`P(in LF)`). Ниже эффекты на занятость вычисляются по стандартной"
    " пробит-производной $\\gamma_v\\,\\phi(Z'\\gamma)$, усреднённой по выборке.",
)

_set(
    67,
    "Строка `educ` интерпретируется так: дополнительный год обучения"
    " повышает *условный логарифм зарплаты среди работающих* на величину"
    " из столбца `cond E[lwage]` и *одновременно* повышает вероятность"
    " занятости на величину из столбца `P(in LF)`.\n"
    "\n"
    "Переменные `nwifeinc`, `age`, `kidslt6`, `kidsge6`, входящие"
    " только в уравнение отбора, тоже появляются в столбце"
    " `cond E[lwage]`, но с малыми и статистически незначимыми"
    " значениями. Это **не** пустые ячейки: данные переменные влияют"
    " на условную зарплату через обратный коэффициент Миллса"
    " $-\\gamma_v\\,\\rho\\,\\sigma\\,\\delta(Z'\\gamma)$."
    " На данных Mroz оценённая корреляция $\\hat\\rho$ близка к нулю"
    " (около $0.03$), поэтому этот канал численно мал — что и ожидается"
    " при слабой поправке на отбор.",
)


_set(
    68,
    "---\n\n## 4.3 Эмпирическая проверка корректности\n"
    "\n"
    "Три независимые проверки оценщика модели Хекмана:\n"
    "\n"
    "1. **Проверка на сгенерированных данных** — показано в §4.2:"
    " на данных с известными параметрами обе оценки (двухшаговая и"
    " совместная ММП) сходятся к истинным значениям.\n"
    "2. **Сравнение с независимыми реализациями** —"
    " `py4etrics.Heckit` (только двухшаговый алгоритм) и"
    " `sampleSelection::selection` из R [7] (двухшаговый алгоритм и"
    " совместная оценка) на тех же данных. Результаты должны совпасть"
    " с нашими с точностью оптимизатора.\n"
    "3. **Численная проверка производных** — производные из"
    " `_heckit_effects.py` сверяются с численным градиентом `predict()`"
    " из `heckit.py`, реализованного независимо.",
)

_set(
    69,
    "## Сравнение с независимыми реализациями (`py4etrics` и `sampleSelection`)\n"
    "\n"
    "Восстановления истинных параметров на сгенерированных данных"
    " недостаточно: ошибочно реализованный оценщик мог бы выдавать"
    " правдоподобные числа и проходить эту проверку. Решающая проверка"
    " — сравнить с независимой реализацией на тех же данных."
    " Будем сравнивать с двумя пакетами:\n"
    "\n"
    "- **`py4etrics.Heckit`** (Hasebe и др.) — Python-реализация"
    " двухшагового алгоритма Хекмана.\n"
    "- **`sampleSelection::selection`** [7] — стандартный R-пакет для"
    " модели отбора Хекмана, реализующий обе версии"
    " (двухшаговый алгоритм и совместный ММП) с той же функцией"
    " логарифмического правдоподобия, что и у нас.\n"
    "\n"
    "Обе проверки включены в тесты"
    " (`tests/test_heckit.py::test_twostep_matches_py4etrics` и"
    " `tests/test_r_reference.py::test_heckit_mle_matches_R`); ячейки"
    " ниже воспроизводят эти сравнения.",
)

_set(
    70,
    "Оба пакета вычисляют двухшаговые оценки по одним и тем же"
    " аналитическим формулам, поэтому расхождение порядка $10^{-6}$ —"
    " погрешность округления в операциях линейной алгебры"
    " numpy / statsmodels.",
)

_set(
    71,
    "Два независимых оптимизатора: scipy `L-BFGS-B` (в нашей реализации"
    " минимизирует отрицательный логарифм правдоподобия) и R-функция"
    " `maxLik` (максимизирует логарифм правдоподобия) — решают одну и ту"
    " же задачу: $\\arg\\min(-\\log L) = \\arg\\max(\\log L)$."
    " Эмпирическое совпадение оценок параметров, $\\sigma$, $\\rho$ и"
    " значения $\\log L$ в точке оптимума (в пределах точности"
    " оптимизаторов, порядка $10^{-3}$) подтверждает корректность нашей"
    " реализации.",
)

_set(
    72,
    "**Численная проверка производных с помощью `predict`.** Аналитические"
    " производные в `_heckit_effects.py` реализованы независимо от функции"
    " `predict()` в `heckit.py`. Поэтому численная производная (центральная"
    " разность), вычисленная через `predict()`, служит независимой"
    " проверкой аналитического расчёта. Вычисления проводятся в точке"
    " средних значений выборки ($X = \\bar{X}$, $Z = \\bar{Z}$) путём"
    " последовательного сдвига каждого регрессора на малую величину.",
)

_set(
    73,
    "Значения в столбцах совпадают до семи-восьми знаков: аналитические"
    " формулы производных в `_heckit_effects.py` согласованы с формулами"
    " прогнозов в `heckit.py`.",
)


# ----------------------------------------------------------------------
# Bibliography (new last cell — index 74)
# ----------------------------------------------------------------------


_set(
    74,
    "---\n\n## Список литературы\n\n"
    "[1] Tobin J. Estimation of Relationships for Limited Dependent Variables //"
    " *Econometrica*. 1958. Vol. 26, no. 1. P. 24–36.\n\n"
    "[2] Olsen R. J. A Note on the Uniqueness of the Maximum Likelihood"
    " Estimator for the Tobit Model // *Econometrica*. 1978. Vol. 46, no. 5."
    " P. 1211–1215.\n\n"
    "[3] Heckman J. J. Sample Selection Bias as a Specification Error //"
    " *Econometrica*. 1979. Vol. 47, no. 1. P. 153–161.\n\n"
    "[4] Greene W. H. On the Asymptotic Bias of the Ordinary Least Squares"
    " Estimator of the Tobit Model // *Econometrica*. 1981. Vol. 49, no. 2."
    " P. 505–513.\n\n"
    "[5] Fair R. C. A Theory of Extramarital Affairs //"
    " *Journal of Political Economy*. 1978. Vol. 86, no. 1. P. 45–61.\n\n"
    "[6] Mroz T. A. The Sensitivity of an Empirical Model of Married Women's"
    " Hours of Work to Economic and Statistical Assumptions //"
    " *Econometrica*. 1987. Vol. 55, no. 4. P. 765–799.\n\n"
    "[7] Toomet O., Henningsen A. Sample Selection Models in R: Package"
    " sampleSelection // *Journal of Statistical Software*. 2008. Vol. 27,"
    " no. 7.\n\n"
    "[8] Wooldridge J. M. *Econometric Analysis of Cross Section and Panel"
    " Data*. 2nd ed. MIT Press, 2010.\n\n"
    "[9] Greene W. H. *Econometric Analysis*. 8th ed. Pearson, 2018.\n\n"
    "[10] Hansen B. E. *Econometrics*. Princeton University Press, 2022.\n\n"
    "[11] Mincer J. *Schooling, Experience, and Earnings*. New York:"
    " Columbia University Press for the National Bureau of Economic"
    " Research, 1974."
)


# ----------------------------------------------------------------------
# Driver
# ----------------------------------------------------------------------


def build(execute_pdf: bool = True) -> Path:
    if not SOURCE.exists():
        raise FileNotFoundError(
            f"{SOURCE} not found; run examples/_build_report.py first."
        )
    nb = nbf.read(SOURCE, as_version=4)

    md_idx = 0
    untranslated: list[int] = []
    for cell in nb.cells:
        if cell.cell_type != "markdown":
            continue
        if md_idx in RU_MARKDOWN:
            cell.source = RU_MARKDOWN[md_idx]
        elif md_idx != 0:  # cell 0 is the print-CSS block, leave it alone
            stripped = cell.source.strip()
            if stripped and not stripped.startswith("---"):
                untranslated.append(md_idx)
        md_idx += 1

    nbf.write(nb, TARGET_NB)
    print(f"wrote {TARGET_NB.name}  ({md_idx} markdown cells inspected, "
          f"{len(RU_MARKDOWN)} translated, {len(untranslated)} untranslated)")
    if untranslated:
        print(f"  untranslated markdown cells: {untranslated}")

    if not execute_pdf:
        return TARGET_NB

    cmd = [
        sys.executable, "-m", "nbconvert", "--to", "webpdf",
        "--output", TARGET_PDF.stem, str(TARGET_NB),
    ]
    print(f"running: {' '.join(cmd)}")
    subprocess.run(cmd, check=True)
    print(f"wrote {TARGET_PDF.name}")
    return TARGET_PDF


def main() -> int:
    parser = argparse.ArgumentParser(description=__doc__)
    parser.add_argument("--no-pdf", action="store_true",
                        help="only write the .ipynb, skip the webpdf conversion")
    args = parser.parse_args()
    build(execute_pdf=not args.no_pdf)
    return 0


if __name__ == "__main__":
    sys.exit(main())
